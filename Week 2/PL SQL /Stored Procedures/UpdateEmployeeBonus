CREATE OR REPLACE PROCEDURE UPDATEEMPLOYEEBONUS(DEPT_NAME IN EMPLOYEE.DEPARTMENT%TYPE, PERCENTAGE IN NUMBER) 
AS 
BEGIN 
UPDATE EMPLOYEE 
SET SALARY = SALARY * ( 1 + PERCENTAGE ) 
WHERE DEPARTMENT = DEPT_NAME ; 

COMMIT ; 
END ;
